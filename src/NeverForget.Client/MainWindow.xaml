<Window x:Class="NeverForget.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NeverForget" Height="900" Width="1320" MinHeight="760" MinWidth="1100"
        WindowStartupLocation="CenterScreen" Background="#F4F6FA">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="7" />
            <Setter Property="Margin" Value="0,4,0,10" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="6" />
            <Setter Property="Margin" Value="0,4,0,10" />
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,4,0,10" />
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,18">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="NeverForget" FontSize="30" FontWeight="SemiBold" Foreground="#17213C" />
                <TextBlock Text="Google Calendar events, right on time" Foreground="#65708A" FontSize="14" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Width="390">
                <TextBlock Text="Server address" FontWeight="SemiBold" />
                <TextBox x:Name="ServerUrlTextBox" Margin="0,4,0,0" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="22" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <StackPanel Margin="0,0,14,0">
                        <TextBlock Text="From" FontWeight="SemiBold" />
                        <DatePicker x:Name="FromDatePicker" Width="145" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="To" FontWeight="SemiBold" />
                        <DatePicker x:Name="ToDatePicker" Width="145" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Calendars included" FontWeight="SemiBold" />
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" MaxHeight="65">
                        <ItemsControl ItemsSource="{Binding Calendars}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsSelected}"
                                              Margin="0,6,18,4" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Reload calendars" Click="ReloadCalendarsButton_Click" />
                    <Button Content="Refresh events" Background="#3157D5" Foreground="White"
                            Click="RefreshButton_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="11*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="8*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="12">
                <DataGrid x:Name="EventsGrid" ItemsSource="{Binding Events}" AutoGenerateColumns="False"
                          IsReadOnly="True" SelectionMode="Single" HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal" BorderThickness="0"
                          SelectionChanged="EventsGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Start" Binding="{Binding StartsAtLocal, StringFormat={}{0:g}}" Width="140" />
                        <DataGridTextColumn Header="End" Binding="{Binding EndsAtLocal, StringFormat={}{0:g}}" Width="140" />
                        <DataGridTextColumn Header="Calendar" Binding="{Binding CalendarName}" Width="130" />
                        <DataGridTextColumn Header="Event" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Notify" Binding="{Binding Notification}" Width="105" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="10" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock x:Name="EditorHeading" Text="New event" FontSize="20"
                                       FontWeight="SemiBold" Margin="0,0,0,14" />
                            <TextBlock Text="Calendar" FontWeight="SemiBold" />
                            <ComboBox x:Name="CalendarComboBox" ItemsSource="{Binding WritableCalendars}"
                                      DisplayMemberPath="DisplayName" />
                            <TextBlock Text="Title" FontWeight="SemiBold" />
                            <TextBox x:Name="TitleTextBox" MaxLength="120" />
                            <TextBlock Text="Description" FontWeight="SemiBold" />
                            <TextBox x:Name="DescriptionTextBox" MaxLength="8000" MinHeight="82"
                                     AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
                            <TextBlock Text="Location" FontWeight="SemiBold" />
                            <TextBox x:Name="LocationTextBox" MaxLength="500" />
                            <CheckBox x:Name="AllDayCheckBox" Content="All-day event" FontWeight="SemiBold"
                                      Margin="0,2,0,10" Checked="AllDayCheckBox_Changed"
                                      Unchecked="AllDayCheckBox_Changed" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Start date" FontWeight="SemiBold" />
                                    <DatePicker x:Name="StartDatePicker" />
                                    <TextBlock x:Name="StartTimeLabel" Text="Start time (HH:mm)" FontWeight="SemiBold" />
                                    <TextBox x:Name="StartTimeTextBox" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="End date" FontWeight="SemiBold" />
                                    <DatePicker x:Name="EndDatePicker" />
                                    <TextBlock x:Name="EndTimeLabel" Text="End time (HH:mm)" FontWeight="SemiBold" />
                                    <TextBox x:Name="EndTimeTextBox" />
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="Notify before event (minutes)" FontWeight="SemiBold" />
                            <TextBox x:Name="NotificationMinutesTextBox" Text="10" />
                        </StackPanel>
                    </ScrollViewer>
                    <WrapPanel Grid.Row="1" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Clear" Click="ClearButton_Click" />
                        <Button x:Name="SaveButton" Content="Add event" Background="#3157D5"
                                Foreground="White" Click="SaveButton_Click" />
                    </WrapPanel>
                </Grid>
            </Border>
        </Grid>

        <TextBlock x:Name="StatusTextBlock" Grid.Row="3" Margin="2,12,0,0" Foreground="#65708A" />
    </Grid>
</Window>
