<Window x:Class="NeverForget.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NeverForget.Client"
        Title="NeverForget" Height="880" Width="1260" MinHeight="740" MinWidth="1050"
        WindowStartupLocation="CenterScreen" Background="#F4F6FA">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="7" />
            <Setter Property="Margin" Value="0,4,0,10" />
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,4,0,10" />
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,18">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="NeverForget" FontSize="30" FontWeight="SemiBold" Foreground="#17213C" />
                <TextBlock Text="Your reminders, right on time" Foreground="#65708A" FontSize="14" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Width="390">
                <TextBlock Text="Server address" FontWeight="SemiBold" />
                <TextBox x:Name="ServerUrlTextBox" Margin="0,4,0,0" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="14" Margin="0,0,0,14">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <StackPanel Margin="0,0,14,0">
                        <TextBlock Text="From" FontWeight="SemiBold" />
                        <DatePicker x:Name="FromDatePicker" Width="150" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="To" FontWeight="SemiBold" />
                        <DatePicker x:Name="ToDatePicker" Width="150" />
                    </StackPanel>
                </StackPanel>
                <Button Content="Refresh list" DockPanel.Dock="Right" HorizontalAlignment="Right"
                        VerticalAlignment="Center" Background="#3157D5" Foreground="White"
                        Click="RefreshButton_Click" />
            </DockPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="12">
                <DataGrid x:Name="RemindersGrid" ItemsSource="{Binding Reminders}" AutoGenerateColumns="False"
                          IsReadOnly="True" SelectionMode="Single" HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal" BorderThickness="0"
                          SelectionChanged="RemindersGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Occurrence" Binding="{Binding OccursAtLocal, StringFormat={}{0:g}}" Width="145" />
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Schedule" Binding="{Binding Schedule}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="10" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock x:Name="EditorHeading" Text="New reminder" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,14" />
                            <TextBlock Text="Title" FontWeight="SemiBold" />
                            <TextBox x:Name="TitleTextBox" MaxLength="120" />
                            <TextBlock Text="Message" FontWeight="SemiBold" />
                            <TextBox x:Name="MessageTextBox" MaxLength="2000" MinHeight="110"
                                     AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
                            <CheckBox x:Name="IsRecurringCheckBox" Content="Repeat this reminder"
                                      FontWeight="SemiBold" Margin="0,3,0,12"
                                      Checked="IsRecurringCheckBox_Changed" Unchecked="IsRecurringCheckBox_Changed" />
                            <StackPanel x:Name="OneTimeSchedulePanel">
                                <TextBlock Text="Reminder date" FontWeight="SemiBold" />
                                <DatePicker x:Name="ScheduledDatePicker" />
                                <TextBlock Text="Time (HH:mm)" FontWeight="SemiBold" />
                                <TextBox x:Name="ScheduledTimeTextBox" Text="09:00" />
                            </StackPanel>
                            <local:CronEditor x:Name="ScheduleEditor" Visibility="Collapsed" />
                        </StackPanel>
                    </ScrollViewer>
                    <WrapPanel Grid.Row="1" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Clear" Click="ClearButton_Click" />
                        <Button x:Name="DeleteButton" Content="Delete" Foreground="#A11D2C"
                                Visibility="Collapsed" Click="DeleteButton_Click" />
                        <Button x:Name="SaveButton" Content="Add" Background="#3157D5" Foreground="White"
                                Click="SaveButton_Click" />
                    </WrapPanel>
                </Grid>
            </Border>
        </Grid>

        <TextBlock x:Name="StatusTextBlock" Grid.Row="3" Margin="2,12,0,0" Foreground="#65708A" />
    </Grid>
</Window>
